<p><%= link_to "My profile", root_path%> </p>
<h1>Welcome to Course</h1>
<% if current_user %>
  <p>Hello <%=current_user.email %></p>
  <%= link_to "sign out", destroy_user_session_path, method: :delete %>
<%else  %>
  <p>You are not signed in </p>
  <p>Please Sign in</p>
  <%= link_to "Sign in", new_user_session_path %>
  <%= link_to "Sign up", new_user_registration_path %>
<%end%>
<div>
  <p>
    Filter courses
    <%= form_with url: courses_path, method: :get do |form| %>
      <%= form.text_field :query %>
      <%= form.submit "Filter" %>
    <% end %>
  </p>
</div>
<div class="mt-5">
  <h1>list of all the available course</h1>
  <hr>
  <% @courses.each.with_index(1) do |course,index|%>
    <p><%=index%>. Name of the course : <%=link_to  course.name, course_path(course.id) %></p>
  <% end %>
</div>
<div>
</div>
